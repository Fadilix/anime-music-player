type trackType = {
    name: string,
    src: string, 
    bg: string
}

const MusiCard = (track: trackType) => {

    return (
        <div>
            
        </div>
    )
}

export default MusiCard